# ===========================
# WhatsApp RH Bot (Demo) - .env.example
# Copie para um arquivo ".env" e preencha com seus valores.
# NUNCA faça commit do arquivo .env
# ===========================

# Porta do servidor (local)
PORT=3000

# Token de verificação do webhook (Meta)
VERIFY_TOKEN=coloque_seu_verify_token_aqui

# Token de acesso da WhatsApp Cloud API (Meta)
WHATSAPP_TOKEN=coloque_seu_whatsapp_token_aqui

# Phone Number ID da WhatsApp Cloud API (Meta)
PHONE_NUMBER_ID=coloque_seu_phone_number_id_aqui

# Número de teste (formato E.164 sem +) — usado no endpoint /test-message
TEST_NUMBER=55DDDNXXXXXXXX

# E-mail para notificação de fila (SMTP)
SMTP_USER=seu_email@dominio.com
SMTP_PASS=sua_senha_ou_app_password

# Envio de notificação: remetente e destinatários
NOTIFY_FROM=seu_email@dominio.com
NOTIFY_TO=destinatario1@dominio.com,destinatario2@dominio.com

# Debug (0/1) — habilita logs adicionais
DEBUG=0

# Opcional: espaçamento mínimo entre envios (ms)
MIN_GAP_MS=0
